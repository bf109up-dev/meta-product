# Inherit poky as the base distro
require conf/distro/poky.conf

# Override variables for our custom distro
DISTRO = "product"
DISTRO_NAME = "product AI Agent Distro"
DISTRO_VERSION = "1.0.0"
DISTRO_CODENAME = "ai-core"

# Additional system-wide configurations can be added here
# e.g., INIT_MANAGER = "systemd"
